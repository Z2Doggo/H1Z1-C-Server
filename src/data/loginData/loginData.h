typedef enum BAN_INFO
{
    LOCAL_BAN = 1,
    GLOBAL_BAN = 2,
    VPN = 3,
    HWID = 4,
    UNVERIFIED = 5,
} BAN_INFO;

typedef enum NAME_VALIDATION_STATUS
{
    AVAILABLE = 1,
    TAKEN = 2,
    INVALID = 3,
    PROFANE = 4,
    RESERVED = 5,
} NAME_VALIDATION_STATUS;

typedef enum LOGIN_KICK_REASON
{
    UNDEFINED = 0,
    GLOBAL_BAN_REASON = 1,
    ASSET_VALIDATION = 2,
} LOGIN_KICK_REASON;